(function contruct_board who
    (let board (vec []))
    (for x range(10)
        (let board (set-at board x (vec []))
        (for y range(10))
    )
    ({who: })
)

(function ms x y owner
    (print (y (x board)))
)

(ms 1, 1, "fcat#9000")