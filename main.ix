(function construct_board who
    (let board (vec []))
    (for x range(10)
        (let board (set-at board x (vec [])))
        (for y range(10))
    )
)

(function ms x y owner
    (let board (construct_board owner))
    (print (y (x board)))
)

(ms 1, 1, "fcat#9000")